<div class="title_box">
   Add A Business
</div>
  <%= form_with(model: business) do |form| %>
    <% if business.errors.any? %>
      <div id="error_explanation">
        <h2><%= pluralize(business.errors.count, "error") %> prohibited this business from being saved:</h2>

        <ul>
          <% business.errors.each do |error| %>
            <li><%= error.full_message %></li>
          <% end %>
        </ul>
      </div>
    <% end %>

    <div class="field">
      <%= form.label :business_name %>
      <%= form.text_field :business_name %>
    </div>

    <div class="field">
      <%= form.label :about_text, "Brief Introduction"%>
      <%= form.text_field :about_text , value: 'Work Build and Teach'%>
    </div>

    <div class="field">
      <%= form.label :business_email %>
      <%= form.email_field :business_email ,autocomplete: "email" %>
    </div>
    
    

    <div class="field">
      <%= form.label :description %> 
      <%= form.rich_text_area :description , class: "form-control customized-min-height", value:"Tell As About Your Business"%>
    </div>

    
          <!--h3 class=space_title>Add Address</h3>
          <div id='tasks'>
            <%#= form.fields_for :addresses do |add| %>
              <%#= render 'address_fields', :f => add %>
            <%# end %>
            <div class='links add_address'>
              <%#= link_to_add_association 'Add Address', form, :addresses%>
            </div>
          </div-->
    <div class="field_submit">
      <%= form.submit" CONTINUE",  class: "btn-success"%>
    </div>
  <% end %>

